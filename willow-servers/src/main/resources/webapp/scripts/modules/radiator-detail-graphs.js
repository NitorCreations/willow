Box.Application.addModule('radiator-detail-graphs', function(context) {
  'use strict';

  /**
   * Currently doesn't do anything, but in future manages
   * constructing nv-graph grid for the particular host
   */

  return {
    init: function() {},
    destroy: function() {}
  };
});
